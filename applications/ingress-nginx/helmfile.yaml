releases:
- name: ingress-nginx
  namespace: ingress-nginx
  chart: ingress-nginx
  wait: true
  installed: {{ .Values.ingress.enabled }}  # Ingress 모드일 때만 설치
  labels:
    app: ingress-nginx
  values:
  - values.yaml
